<?php

    if(isset($_GET['expanded']))
    {
        if($_GET['expanded'] == '1')
        {
            define('UI_EXPANDED', true);
        }
        else
        {
            define('UI_EXPANDED', false);
        }
    }
    else
    {
        if(DYNAMICAL_CLIENT_IS_MOBILE)
        {
            define('UI_EXPANDED', true);
        }
        else
        {
            define('UI_EXPANDED', false);
        }
    }